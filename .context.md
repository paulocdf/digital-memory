# Digital Memory — Development Context

> This file is a compaction context for AI coding assistants. Read this file at the start of each session to understand the project state.

## Goal

Migrate a Hugo-based personal knowledge garden ("Digital Memory") from static markdown content to a **fully dynamic, Firestore-backed, local-first architecture**. All content (books, inbox, topics, snippets) lives in Firestore, is cached locally in IndexedDB, and rendered dynamically via client-side JavaScript (marked.js + highlight.js). The site uses the `hugo-book` theme (git submodule, forked to `paulocdf/hugo-book`).

On top of the core architecture, we built a **time-tracked TODO system** (Inbox page), **Dashboard** (analytics), **D3.js graph** (landing page), **notification/reminder system** (in-page banners), **Quick Capture modal** (Note/Code/Todo modes), and an **11-feature enhancement plan** (all 11 complete and pushed).

## Architecture

- **Local-first with Firestore sync**: On login, fetch all notes from Firestore, store in IndexedDB. Subsequent loads read from IndexedDB (instant). Sync on login + periodic background sync every 5 minutes.
- **Dual-write**: All data writes go to both Firestore and IndexedDB via centralized `firestoreWrite()`.
- **Offline write support** (Feature 4): IDB-first optimistic writes, auto-queue when offline, drain on reconnect. Client-side ID generation via `.doc().id` for offline-compatible creates.
- **Sync mechanism**: `window.dmSync` API with ~40+ methods; custom events (`dm-sync-complete`, `dm-todos-updated`, `dm-attachments-updated`, `dm-review-updated`) trigger UI re-renders.
- **Modal pattern**: Hugo partials included in `baseof.html` via `inject/body.html`; expose `window.dmXxx = { open, close }` global API.
- **Script loading order in baseof.html**: Firebase SDK + init in `<head>` (synchronous — required because inline init scripts depend on `firebase` global), page content in `<body>`, then `dm-sync.html` partial, then `inject/body.html` partial.
- **Firebase SDK cannot be deferred** — inline `firebase.initializeApp()` in `inject/head.html` runs synchronously and depends on the SDK scripts being loaded first.
- **Auth**: Google Auth required to view all dynamic content (Firebase Auth with Google provider).
- **User isolation**: Each user only sees their own content. IndexedDB cache cleared on sign-out/user switch. Firestore queries filter by `userId`. Security rules enforce server-side isolation.
- **Books**: Flat entries under Books in sidebar (one entry per book, NO sub-grouping by bookTitle).
- **Snippets**: Grouped by `language` field in sidebar.
- **Inbox**: Single-note page model. Quick Capture with destination=Inbox has append/new toggle.
- **Topics**: Flat list of clickable entries in sidebar.
- **Search**: Indexes from IndexedDB, NOT from Hugo's static `search-data.json`.
- **Note viewer**: `/docs/view/?id=<firestoreDocId>` renders full note content.
- **Quick Capture**: Dual-write. Has Note/Code/Todo modes. Tab key cycles modes.
- **Right sidebar (ToC)**: Client-side ToC builder scans rendered DOM for headings.
- **D3 Graph**: Builds data from IndexedDB.
- **Edit Modal**: Shared partial (`note-edit-modal.html`), exposed as `window.dmEditModal = { open, close }`.

## User Design Decisions

- **Separate Firestore `todos` collection** — not inline markdown metadata
- **Inbox only** — time-tracked TODOs only appear on the Inbox page
- **Freeform tags** for categories — same pattern as note tags (type + Enter)
- **One level of subtask nesting** — parent TODOs can have child TODOs, but children cannot have children
- **Auto-complete parent** — when all children are done, parent auto-completes with actualMin = sum of children
- **Minutes input** — users enter minutes directly; Pomodoro conversion (÷25) is a display concern
- **Default 25 minutes** — if estimate input is left blank, tasks default to 25 minutes
- **Done tasks stay in place** — when marked done, tasks remain inline in the list
- **Dashboard panels** — ALL 5: Estimated vs Actual comparison, Completed tasks summary, Category breakdown, Daily/weekly activity log, Pomodoro equivalents
- **Delete on all tasks** — trash icon shows on both active and completed tasks
- **Clear All Completed** — bar at bottom shows count of completed tasks + "Clear all" button
- **Reminder approach**: Custom in-page persistent banner (NOT OS Notification API)
- **Reminder banner**: Full-width fixed top banner, stays until user clicks "Acknowledge"
- **Notification sound**: Web Audio API chime (3-note ascending sine waves)
- **Quick Capture Todo mode**: 3rd mode with full fields (title, estimate, date, reminder)
- **Quick Capture UI**: Contextual second row, Tab/Shift+Tab cycles modes
- **Backlink mechanism**: Both wikilinks `[[Note Title]]` (primary) AND title matching (secondary, min 4 chars)
- **Backlink display**: "Linked from" section at bottom of note viewer with explicit/mention badges
- **Spaced repetition algorithm**: SM-2 (SuperMemo 2) with quality ratings 0-5
- **Review queue location**: Dedicated Review page at `/docs/review/`
- **Opting notes in**: Explicit opt-in per note via book icon button
- **Review UX**: Show title first, reveal content on click, then rate recall quality
- **Pomodoro timer toast is draggable** via mouse/touch on header
- **Soft delete field approach** for trash (not separate collection)
- **Trash page** at `/docs/trash/`
- **Client-side auto-purge** of items trashed 30+ days

## Data Models

### Notes (`notes` collection)
```javascript
{
  userId, userEmail, userName, title, content, mode ("note"/"code"),
  language, destination ("inbox"/"topic"/"book-note"/"snippets"),
  bookTitle, tags, pinned, deletedAt, createdAt, updatedAt
}
```

### TODOs (`todos` collection)
```javascript
{
  id, userId, title, estimatedMin, actualMin, category,
  done, parentId, order, scheduledDate, reminderAt, reminderFired,
  createdAt, updatedAt, completedAt
}
```

### Review Cards (`reviewCards` collection)
```javascript
{
  id, noteId, userId,
  easeFactor: 2.5,     // SM-2 easiness factor (min 1.3)
  interval: 0,         // days until next review
  repetitions: 0,      // successful reviews count
  nextReviewAt,        // timestamp: when next review is due
  lastReviewedAt, createdAt, updatedAt
}
```

### Note Versions (`noteVersions` collection)
```javascript
{
  id, noteId, userId, title, content, tags, createdAt
}
```
- Immutable snapshots (no update rule). Max 50 per note, auto-pruned on save.
- Saved BEFORE edit is applied (captures previous state).

### Attachments (`attachments` collection)
```javascript
{
  id, noteId, userId, fileName, fileType, fileSize,
  storagePath, downloadUrl, createdAt
}
```
- Immutable metadata. Files in Firebase Storage at `users/{userId}/attachments/{noteId}/{timestamp}-{filename}`.
- 10 MB max, content type whitelist: images, PDF, text, JSON, ZIP.

### SM-2 Algorithm
- Quality ratings: 0-5 (0=complete blackout, 5=perfect response)
- If quality >= 3: interval increases (1d → 6d → prev_interval * EF)
- If quality < 3: interval resets to 1d, repetition count resets
- EF = max(1.3, EF + (0.1 - (5-q)*(0.08 + (5-q)*0.02)))
- Next review date = today + interval days

## Firebase Config

Project `digital-memory-0`, initialized in `inject/head.html` with globals `window.dmAuth`, `window.dmDb`, `window.dmGoogleProvider`. Uses compat v9 SDK (v10.14.1). Storage bucket: `digital-memory-0.firebasestorage.app`. SDK scripts: `firebase-app-compat.js`, `firebase-auth-compat.js`, `firebase-firestore-compat.js`, `firebase-storage-compat.js` from gstatic CDN.

## 11 Features Plan (Implementation Order)

| # | Feature | Status |
|---|---------|--------|
| 1 | Fix Tags page | DONE |
| 2 | Fix Backlinks | DONE |
| 3 | Pomodoro timer | DONE |
| 4 | Offline write support | DONE |
| 5 | Note version history | DONE |
| 6 | Image/file attachments | DONE |
| 7 | Spaced repetition / review queue | DONE |
| 8 | Keyboard shortcuts overlay | DONE |
| 9 | Note pinning | DONE |
| 10 | Trash / soft delete | DONE |
| 11 | Search index persistence | DONE |

### Feature Details (Completed)

**Feature 1 — Fix Tags Page:**
- Added `getAllTags()` and `getNotesByTag()` to `dmSync` API
- Rewrote `tag-cloud.html` — dynamic rendering from IndexedDB with auth gating, loading/empty states, clickable tag pills, edit/delete, live updates
- Added CSS for `.tag-pill-active`, `.tag-cloud-container`, `.tag-cloud-filter-info`

**Feature 2 — Fix Backlinks:**
- Deleted dead `component/backlinks.html` partial
- Added `getBacklinksForNote(noteId)` to `dmSync` API
- Added `marked.use()` wikilink extension in `html-head.html`
- Added `window.buildWikilinkMap()` + auto-build on `dm-sync-complete`
- Added "Linked from" backlinks section in note viewer with explicit/mention badges
- Added wikilink + title mention edge scanning in `graph.js`
- Added CSS for `.backlinks-section`, `.backlink-card`, `.wikilink`, `.wikilink-missing`

**Feature 3 — Pomodoro Timer:**
- Created `pomodoro-timer.html` partial with floating timer widget (fixed, bottom-right)
- 25-min work / 5-min break cycles, session counting, accumulated time tracking
- Play/pause/reset/skip controls, progress bar, phase indicator
- Web Audio API chime on phase completion
- `window.dmPomodoro` global API: `start(todoId, title)`, `stop()`, `isActive(todoId)`, `getActiveTodoId()`
- `dm-pomodoro-stopped` custom event with `{ todoId, trackedMinutes }`
- Timer play button on each active todo item
- Pre-populate completion modal with tracked Pomodoro time

**Feature 4 — Offline Write Support:**
- Added `writeQueue` IDB object store (autoIncrement key)
- Added `_isOnline` state, queue primitives: `queueWrite()`, `getAllQueued()`, `removeQueued()`, `clearQueue()`
- Added `firestoreWrite(opts)` — centralized IDB-first optimistic writes, auto-queue offline
- Added `drainQueue()` — FIFO processing on reconnect
- Added online/offline event listeners + offline indicator pill
- Converted all 20 Firestore write sites across 9 files to use `firestoreWrite()`
- Converted `.add()` calls to client-side `.doc().id` + `.set()` for offline ID generation

**Feature 5 — Note Version History:**
- Added `noteVersions` Firestore collection with immutable-snapshot security rules
- Added `noteVersions` IDB store with `noteId` and `createdAt` indexes
- Auto-saves version snapshot before each edit (skips if unchanged)
- Max 50 versions per note with automatic oldest-first pruning
- Created `version-history-modal.html` with sidebar list + LCS diff/full-content view
- Restore + Delete functionality. History button (clock icon) in edit modal, note viewer, single-note

**Feature 6 — Image/File Attachments:**
- Firebase Storage SDK added, init in inject/head.html
- Storage rules reference file created
- IDB store with noteId/createdAt indexes
- Full dm-sync API: upload, get, delete, bulk delete, sync
- Edit modal: paperclip button, drag-drop, paste, progress bar, auto-insert markdown, gallery
- Viewer + single-note: attachment display sections

**Feature 7 — Spaced Repetition / Review Queue:**
- Firestore rules: `reviewCards` collection — create/read/update/delete, owner-only
- IDB store (version 7): `reviewCards` with `noteId` and `nextReviewAt` indexes
- SM-2 algorithm: `sm2()` function computing easeFactor, interval, repetitions, nextReviewAt
- dm-sync API: `createReviewCard()`, `reviewCard(cardId, quality)`, `deleteReviewCard()`, `getReviewCardForNote()`, `getReviewCards(dueOnly)`, `getDueCount()`, `syncReviewCards()`
- `dm-review-updated` custom event on card create/review/delete
- Note-viewer + single-note: Book icon button toggles SR enrollment, `.note-action-btn-active` class
- Review page (`content/docs/review.md`) with `review-queue` shortcode
- Full flashcard UI: auth gate, stats bar, progress bar, reveal/rate/next flow
- Keyboard shortcuts: Space=reveal, 0-5=rate, Enter=next
- Schedule list showing all enrolled notes with due dates and EF values
- Sidebar menu updated with "Review" link

**Feature 8 — Keyboard Shortcuts Overlay:**
- Created `keyboard-shortcuts.html` partial, included via `inject/body.html`
- `?` key opens overlay (global, not in input fields)
- Organized by context: Global, Search, Quick Capture, Edit, Review, Lightbox
- `window.dmKeyboardShortcuts = { open, close }` API
- CSS in `_custom.scss`

**Feature 9 — Note Pinning:**
- `pinned` boolean field on notes (no Firestore rules change needed)
- Pin/unpin button (thumbtack icon) on note-viewer, single-note, section-notes
- `.note-action-btn-active` class for pinned state
- Sidebar sorts pinned notes first within each section
- `serializeNote()` preserves `pinned` field during Firestore→IDB sync

**Feature 10 — Trash / Soft Delete:**
- `deletedAt` timestamp field on notes (null = active, set = trashed)
- Delete buttons now soft-delete (set `deletedAt`) instead of permanent delete
- `getActiveNotes()` filters out trashed notes; `getAllNotes()` delegates to it
- `getTrashedNotes()`, `restoreNote()`, `permanentDeleteNote()`, `emptyTrash()` APIs
- `getNoteIncludingTrashed(id)` for viewing trashed notes
- Trash page at `/docs/trash/` with `trash-list` shortcode
- Restore, permanent delete, empty trash functionality
- Client-side auto-purge of items trashed 30+ days (runs after each sync)
- Sidebar has "Trash" entry at bottom
- `serializeNote()` preserves `deletedAt` field during Firestore→IDB sync

**Feature 11 — Search Index Persistence:**
- FlexSearch v0.6.30 `.export()`/`.import()` API for serialization
- Index cached in IDB `meta` store with key `'searchIndex'`
- Validated by `noteCount` comparison on load
- `dm-sync-complete` invalidates cache and rebuilds
- `dmSync.getSearchIndexCache()`, `putSearchIndexCache()`, `clearSearchIndexCache()` APIs

## Keyboard Shortcuts Inventory (for Feature 8)

**Global shortcuts (work anywhere, not in input fields):**
| Key | Action | File |
|-----|--------|------|
| `?` | Open shortcuts overlay | TO BE IMPLEMENTED |
| `E` | Open Quick Capture | `inject/body.html:1200` |
| `[` | Toggle sidebar | `inject/body.html:93` |
| `Ctrl/Cmd+K` | Open search | `search.js:271` |
| `S` or `/` | Open search | `search.js:282` |

**Search modal:**
| Key | Action | File |
|-----|--------|------|
| `Escape` | Close search | `search.js:292` |
| `ArrowDown/Up` | Navigate results | `search.js:298-304` |
| `Enter` | Open selected result | `search.js:310` |
| `Tab` | Select highlighted | `search.js:326` |

**Quick Capture modal:**
| Key | Action | File |
|-----|--------|------|
| `Escape` | Close modal | `inject/body.html:1209` |
| `Ctrl/Cmd+Enter` | Save | `inject/body.html:1213` |
| `Tab`/`Shift+Tab` | Cycle modes (Note/Code/Todo) | `inject/body.html:1218` |
| `Ctrl/Cmd+B` | Bold | `inject/body.html:1229` |
| `Ctrl/Cmd+I` | Italic | `inject/body.html:1232` |
| `Ctrl/Cmd+E` | Inline code | `inject/body.html:1235` |
| `Ctrl/Cmd+Shift+T` | Todo checkbox | `inject/body.html:1238` |
| `Ctrl/Cmd+Shift+H` | Heading | `inject/body.html:1241` |

**Edit modal:**
| Key | Action | File |
|-----|--------|------|
| `Escape` | Close modal | `note-edit-modal.html:832` |
| `Ctrl/Cmd+Enter` | Save | `note-edit-modal.html:836` |
| `Ctrl/Cmd+B/I/E` | Bold/Italic/Code | `note-edit-modal.html:818-820` |
| `Ctrl/Cmd+Shift+H/T` | Heading/Todo | `note-edit-modal.html:823-824` |
| `Enter` in tag input | Add tag | `note-edit-modal.html:795` |
| `Backspace` in empty tag input | Remove last tag | `note-edit-modal.html:804` |

**Review page:**
| Key | Action | File |
|-----|--------|------|
| `Space` | Reveal content | `review-queue.html:721` |
| `0-5` | Rate recall quality | `review-queue.html:727-732` |
| `Enter`/`Space` | Next card (after rating) | `review-queue.html:736` |

**Image lightbox:**
| Key | Action | File |
|-----|--------|------|
| `Escape` | Close lightbox | `html-head.html:412` |
| `+`/`=` | Zoom in | `html-head.html:413` |
| `-`/`_` | Zoom out | `html-head.html:414` |
| `0` | Fit to view | `html-head.html:415` |

## Key Technical Discoveries

1. **Hugo shortcode comments parse template delimiters**: An HTML comment `<!-- Usage: {{< single-note >}} -->` causes Hugo to try to parse `{{<` as a template action.
2. **ToC builder needs `.single-note-body` selector**: Checks `.note-viewer-body`, `.single-note-body`, `.section-notes-list`, `article.markdown`.
3. **marked.js v15 renderer API**: Uses `renderer.heading = function(data)` where `data` has `.text` and `.depth`.
4. **Hugo `jsonify` double-encodes with `--minify`**: Fix: use `{{ $notes | jsonify | safeJS }}`.
5. **Firestore rejects `undefined` values**: Must pass explicit `null`.
6. **`baseURL` includes `/digital-memory/`**: All URLs prefixed with `/digital-memory/`.
7. **Edit modal styles are inline in `note-edit-modal.html`**, NOT in `_custom.scss`.
8. **Quick Capture styles are in `_custom.scss`**, HTML in `quick-capture-modal.html`, JS in `body.html`.
9. **`.note-action-btn` class defined in both `single-note.html` and `note-viewer.html`** inline styles.
10. **marked.js v15 renders task list checkboxes** as `<li><input checked="" disabled="" type="checkbox">` — `disabled` must be removed for interactivity.
11. **ToC scroll spy race condition**: Must defer `update()` via double `requestAnimationFrame`.
12. **Bundled `mermaid.min.js` is v9.2.0**: Uses `mermaid.init()`, NOT `mermaid.run()`.
13. **Kroki PlantUML SVGs have hardcoded small dimensions**: Must strip attributes AND inline style dimensions.
14. **marked.js `renderer.code` override must be set BEFORE `marked.setOptions()`**.
15. **`--surface-bg`** was used in 2 places but never defined. Replaced with `--body-background`.
16. **`--accent-blue-hover`** used in `_graph.scss:429` but never defined. Now added to both themes.
17. **Hardcoded colors** replaced with CSS vars where `--color-success`, `--color-danger` exist.
18. **Firebase SDK cannot use `defer`** — inline init runs synchronously.
19. **`content/menu/index.md`** had 7 of 9 links pointing to non-existent pages. Fixed.
20. **Firestore update rules** fixed: now check both `resource.data.userId` and `request.resource.data.userId`.
21. **SRI hashes** added for marked@15.0.7, highlight.js@11.11.1.
22. **Duplicate `@keyframes spin`** removed.
23. **Font-family stack** repeated 14 times in `_graph.scss`. Replaced with `$graph-font-stack`.
24. **`outline: none` without replacement** at 10 locations. 8 converted to `:focus-visible` with ring.
25. **Mermaid** hardcoded `theme: 'dark'`. Now reads `data-theme` attribute.
26. **highlight.js theme switching** now toggles via MutationObserver on `data-theme`.
27. **Diagram dark mode** CSS fixed from `@media (prefers-color-scheme: dark)` to `[data-theme="dark"]`.
28. **`sessionStorage` sidebar cache** now uses Hugo build timestamp for invalidation.
29. **ToC builder** `slugify()` now deduplicates IDs with `-1`, `-2` suffixes.
30. **Tags page** was broken (used Hugo static `$tags`). Now dynamic from Firestore/IDB.
31. **Backlinks partial** was dead code. Deleted.
32. **`window._wikilinkMap`** built on `dm-sync-complete` and lazily before first `marked.parse()`.
33. **Todo-list.html action button CSS** defined inline in the partial, not in `_custom.scss`.
34. **20 total Firestore write call sites** across 9 files — all converted to `firestoreWrite()`.
35. **Two dual-write patterns existed** before Feature 4. All now unified.
36. **IndexedDB version is now 7** (v3→4 writeQueue, v4→5 noteVersions, v5→6 attachments, v6→7 reviewCards).
37. **`.add()` converted to `.doc().id` + `.set()`** for offline support.
38. **Firebase Storage bucket**: `digital-memory-0.firebasestorage.app`.
39. **No `storage.rules` or `firebase.json`** existed — Storage rules managed via Console. Reference file created.
40. **`updateEditPreview`** is the correct function name for refreshing markdown preview in note-edit-modal.
41. **Hugo template parsing gotcha**: `{{ }}` inside `<script>` blocks gets parsed as Go template. JSDoc `@returns {{ field }}` causes build failure.
42. **`.note-action-btn-active`** class used for review toggle and pin toggle button active state.
43. **Pomodoro API**: `start()`, `stop()`, `pause()`, `resume()`, `togglePause()`, `isActive()`, `getActiveTodoId()`, `isTimerRunning()`.
44. **Pomodoro timer dragging**: Uses mousedown/mousemove/mouseup + touch equivalents. Switches from right/bottom to left/top when dragged. Resets on `start()`.
45. **`serializeNote()` must include `pinned` and `deletedAt`** — omitting these fields causes data loss on Firestore→IDB sync.
46. **Offline write queue `serverTimestamp()` corruption** — Firebase sentinel objects lose class identity in IDB structured clone. Fixed with `serializeQueueData()`/`deserializeQueueData()` using `__type` markers.
47. **Legacy corrupt queue recovery** — `deserializeQueueData` detects mangled timestamp fields by name (e.g. `createdAt`, `updatedAt`) and reconstructs proper Firebase types, even for pre-fix entries.
48. **15 `onAuthStateChanged` listeners** across 11 files — all independent, no conflict but heavy on auth state change.
49. **`tag-cloud.html` had broken `signIn()` call** — `window.dmAuth.signIn()` doesn't exist. Fixed to `signInWithPopup()`.
50. **FlexSearch v0.6.30** bundled locally. Uses `FlexSearch.create('balance', config)` with `doc` mode. Has `.export()`/`.import()` serialization API.

## Conversion Pattern Reference (for new Firestore writes)

```javascript
// CREATE pattern (offline-compatible):
var newDocId = window.dmDb ? window.dmDb.collection('xxx').doc().id : ('local-' + Date.now() + '-' + Math.random().toString(36).substr(2, 9));
var localObj = { id: newDocId, /* local fields with Date.now() timestamps */ };
window.dmSync.firestoreWrite({
  collection: 'xxx', docId: newDocId, op: 'set',
  data: firestoreData,  // with serverTimestamp()
  localOp: function() { return window.dmSync.putXxx(localObj); }
});

// UPDATE pattern:
window.dmSync.firestoreWrite({
  collection: 'xxx', docId: obj.id, op: 'update',
  data: { field: value, updatedAt: firebase.firestore.FieldValue.serverTimestamp() },
  localOp: function() { obj.field = value; obj.updatedAt = Date.now(); return window.dmSync.putXxx(obj); }
});

// DELETE pattern:
window.dmSync.firestoreWrite({
  collection: 'xxx', docId: objId, op: 'delete', data: null,
  localOp: function() { return window.dmSync.deleteXxx(objId); }
});
```

## Completed Pre-Feature Work

1. **dm-sync.html** — IndexedDB cache layer + Firestore sync engine
2. **Note viewer** — shortcode + page at `/docs/view/?id=<id>`
3. **Dynamic sidebar** — Inbox first (orange accent), Books flat, Snippets grouped, Topics flat
4. **section-notes shortcode** — Books/Topics/Snippets as clickable cards
5. **Quick Capture** — bookTitle support, dual-write, markdown toolbar, live preview, inbox append/new toggle
6. **Dynamic ToC builder** — Scans multiple selectors
7. **Search rewritten** — Indexes from IndexedDB
8. **Custom marked.js heading renderer** — Anchor links matching Hugo pattern
9. **D3 graph, garden-stats, garden-sections made dynamic**
10. **Shared Edit Modal partial** — toolbar, preview, tags, keyboard shortcuts
11. **single-note.html shortcode** — Auth-gated, renders one note per destination
12. **Inbox converted to single-note model**
13. **Merge button on inbox page**
14. **Append/New toggle in Quick Capture**
15. **Edit/Delete buttons as consistent action icons** — `.note-action-btn` class
16. **Edit modal preview panel** — 1200px wide, 92vh tall, side-by-side on wide screens
17. **Sidebar: Inbox first with accent**
18. **Landing page: top N books/topics**
19. **Sidebar sections expanded by default with top 10 limit**
20. **Note viewer header compacted**
21. **ToC scroll spy fix**
22. **Inbox prepend** — newest on top
23. **User isolation** — IDB cleared on sign-out/user switch
24. **Interactive checkboxes** — `window.dmEnableCheckboxes()`
25. **Sidebar sessionStorage cache** — eliminates flicker
26. **Diagram rendering** — Mermaid (local) + Kroki API (PlantUML, D2, GraphViz, etc.)
27. **Diagram lightbox** — Fullscreen with pan/zoom

## Current Git State

- **Submodule** `themes/hugo-book` on branch `master` — clean, pushed to `origin/master` (latest: `1f8b4d1`)
- **Parent repo** on branch `main` — clean, pushed to `origin/main` (latest: `233330a`)
- All 11 features committed and pushed
- All bug fixes committed and pushed

### Recent Bug Fix Commits (post-feature)

| Submodule Commit | Parent Commit | Fix |
|-----------------|---------------|-----|
| `78b802b` | `d7178fb` | `serializeNote()` now preserves `deletedAt` and `pinned` fields; review queue filters orphaned cards; `dm-review-updated` listener no longer a no-op |
| `25026b9` | `c059388` | Offline write queue serializes Firebase `serverTimestamp()`/`Timestamp` objects for IDB round-trip; unrecoverable queue entries discarded |
| `1f8b4d1` | `233330a` | Legacy corrupt queue entry recovery; max 3 drain retries before auto-clear; dismiss button on syncing indicator; tag-cloud.html `signIn()` bug fix |

## Bug Fixes Applied

1. **`serializeNote()` dropped `deletedAt` and `pinned` fields** — Firestore→IDB sync lost these fields, causing trashed notes to reappear and pinned state to reset. Fixed in `dm-sync.html`.
2. **Offline write queue corrupted Firebase objects** — `serverTimestamp()` sentinels and `Timestamp` objects lost class identity after IDB structured clone. Added `serializeQueueData()`/`deserializeQueueData()` with `__type` markers.
3. **Legacy corrupt queue entries caused infinite retry loop** — Added `deserializeQueueData` recovery for mangled timestamp fields (detects by field name + object shape). Max 3 drain attempts before auto-clearing.
4. **"Syncing N changes..." indicator stuck forever** — Added dismiss button (x) on the indicator. User can manually clear stuck queue. Also auto-clears after 3 failed drain attempts.
5. **`drainQueue` retried unrecoverable errors forever** — `invalid-argument` and `permission-denied` errors now remove the entry from queue (alongside existing `not-found` handling).
6. **`dm-review-updated` event listener was a no-op** — Now reloads the review queue when not in an active review session.
7. **`allCards` count included orphaned cards** — Review queue stats "Total" now filters cards for deleted/missing notes.
8. **Dead code in `formatRelativeDate`** — Removed impossible `absDays === 0` branch.
9. **`tag-cloud.html` called `window.dmAuth.signIn()`** — This method doesn't exist on Firebase auth. Fixed to use `signInWithPopup(dmGoogleProvider)`.
10. **15 `onAuthStateChanged` listeners** — Identified across 11 files. No conflict but noted for awareness.

## Next Steps

All 11 planned features are complete. Potential future work:
- Monitor for additional user-reported bugs
- Consider Firebase Auth emulator support for local development
- Consider reducing the 15 independent `onAuthStateChanged` listeners

## File Map

### Core Config & Rules
- `config.toml` — Hugo config
- `firestore.rules` — Firestore security rules (notes, noteVersions, attachments, reviewCards, todos)
- `storage.rules` — Firebase Storage security rules reference
- `content/menu/index.md` — Sidebar menu definition

### Content Pages
- `content/_index.md` — Landing page
- `content/docs/inbox/_index.md` — Inbox page (todo list)
- `content/docs/dashboard.md` — Dashboard page
- `content/docs/view.md` — Note viewer
- `content/docs/tags.md` — Tags page
- `content/docs/review.md` — Review page (Feature 7)
- `content/docs/trash.md` — Trash page (Feature 10)
- `content/docs/books/_index.md`, `content/docs/topics/_index.md`, `content/docs/snippets/_index.md`
- `content/docs/import-notes.md` — Import/management page

### Theme Partials (`themes/hugo-book/layouts/partials/`)
- **`dm-sync.html`** (~1980 lines) — IndexedDB cache + Firestore sync engine + reminder system + offline queue (with serialize/deserialize helpers) + version history + attachment API + review card API (SM-2) + trash auto-purge. IDB version 7, stores: notes, todos, meta, writeQueue, noteVersions, attachments, reviewCards. Public API: `window.dmSync` with ~40+ methods. Offline indicator with dismiss button.
- **`pomodoro-timer.html`** (~500+ lines) — Floating draggable Pomodoro timer widget with pause/resume/togglePause API
- **`todo-list.html`** (~2100+ lines) — Todo list with offline-compatible writes and Pomodoro integration
- **`todo-complete-modal.html`** (~600 lines) — Completion modal with Pomodoro time pre-fill
- **`note-edit-modal.html`** (~1110 lines) — Note editor with version save hook, history button, drag-drop upload, paste upload, attachment gallery
- **`version-history-modal.html`** (~570 lines) — Version history with LCS diff, restore, delete
- **`keyboard-shortcuts.html`** — Keyboard shortcuts overlay (`?` key). `window.dmKeyboardShortcuts` API.
- **`docs/inject/body.html`** (~1830+ lines) — Quick Capture modal logic, sidebar toggle (pinned-first sorting, Trash entry), ToC builder. Includes all modal partials.
- **`docs/inject/head.html`** (33 lines) — Firebase initialization
- **`docs/html-head.html`** (~706 lines) — Firebase SDK scripts (4), marked.js, highlight.js, SRI hashes, checkbox toggle, wikilink extension, image lightbox
- `quick-capture-modal.html` — HTML/CSS only
- `search-modal.html` — Search modal

### Theme Shortcodes (`themes/hugo-book/layouts/shortcodes/`)
- **`review-queue.html`** (~840 lines) — Flashcard-style review UI with SM-2 rating, stats bar, schedule list
- **`trash-list.html`** (~290 lines) — Trash page with restore, permanent delete, empty trash
- **`single-note.html`** (~630 lines) — Page-level note display with pin/review/history/attachments
- **`note-viewer.html`** (~690 lines) — Single note viewer with pin/review/history/attachments/backlinks
- **`tag-cloud.html`** (~300 lines) — Dynamic tag cloud (soft-delete aware)
- **`section-notes.html`** (~470 lines) — Section note listing with pin buttons
- `graph.html` — D3 visualization
- `dashboard.html` — Analytics dashboard
- `import-notes.html` — Bulk import utility

### SCSS Assets
- **`themes/hugo-book/assets/_custom.scss`** (~3800+ lines)
- `themes/hugo-book/assets/_defaults.scss` — CSS custom properties
- `themes/hugo-book/assets/scss/_graph.scss` — Graph styles

### JavaScript Assets
- **`themes/hugo-book/assets/search.js`** (~370+ lines) — FlexSearch indexing + search with IDB cache via `.export()`/`.import()`
- `themes/hugo-book/assets/js/graph.js` (~1550+ lines)

### Static Assets
- `themes/hugo-book/static/flexsearch.min.js` — FlexSearch v0.6.30 (local copy, not CDN)

### Key Style Locations
- Edit modal styles: **inline in `note-edit-modal.html`**
- Quick Capture styles: **`_custom.scss`**
- Quick Capture HTML: `quick-capture-modal.html`, JS: `body.html`
- Diagram + lightbox styles: **end of `_custom.scss`**
- Todo-list action button CSS: **inline in `todo-list.html`**

### Layout
- **`_default/baseof.html`** — Layout with partial loading order: `dm-sync` → `inject/body`

### Project Root
- **`.context.md`** — This file. Read at start of every session.
- **`firestore.rules`** — Firestore security rules
- **`storage.rules`** — Firebase Storage security rules reference
