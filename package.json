{
  "name": "digital-memory-tests",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "test:report": "playwright show-report",
    "emulators": "firebase emulators:start --project digital-memory-0",
    "emulators:export": "firebase emulators:start --project digital-memory-0 --import=./.emulator-data --export-on-exit",
    "prepare": "husky"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "husky": "^9.1.7"
  }
}
